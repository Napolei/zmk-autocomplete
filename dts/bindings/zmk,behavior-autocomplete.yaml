description: ZMK autocomplete behavior

compatible: "zmk,behavior-autocomplete"

include: zero_param.yaml

properties:
  max-delay-ms:
    type: int
    default: 1000

child-binding:
  properties:
    bindings:
      type: uint32-array
      required: true